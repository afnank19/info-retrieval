{
    Currently, the words are being inserted as is into the global glossary
    They need to be processed. E.g "Daraz: is inserted as is. It should be 
    "Daraz: -> daraz 
}
{
    Need to parse json somehow, probably best to use a lib, or if we use a predictable format, we could stick to txt files
    i.e first two lines begin and end with unique char sequence, so that the title, and link can be extracted!
}
{
    Add stemming, best to use a lib for good results!
}